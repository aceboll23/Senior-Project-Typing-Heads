@{
    ViewData["Title"] = "Private Profile";
}
<div class="container py-5 text-center">
    <i class="bi bi-lock-fill display-1 text-muted"></i>
    <h2 class="mt-3">This profile is private</h2>
    <p class="text-muted">@ViewData["Username"] has set their profile to private.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary mt-2">Back to Home</a>
</div>